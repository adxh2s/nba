repos:
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.4.4
    hooks:
      - id: ruff
        args: [--fix]        # auto-corrige si possible
      - id: ruff-format      # formatte le code automatiquement (si supporté)
